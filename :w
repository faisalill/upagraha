<script>
import { useThrelte } from '@threlte/core';
import { useGltf } from '@threlte/extras';
import { onMount } from 'svelte';

const { scene } = useThrelte();
const satelliteModel = useGltf('/satellite.glb', { useDraco: true});

onMount(()=> {
 satellite.then((val)=>{
  scene.add(val.scene);
  }) 
})
</script>
